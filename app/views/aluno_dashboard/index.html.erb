
<style>
  .dashboard-bg {
    background: #183642;
    border-radius: 2rem;
    box-shadow: 0 8px 32px rgba(0,0,0,0.18);
    padding: 2.5rem 1.5rem 2rem 1.5rem;
    color: #fff;
    max-width: 1100px;
    margin: 2rem auto;
  }
  .dashboard-title {
    font-size: 2.8rem;
    font-weight: 900;
    letter-spacing: 2px;
    color: #ffe066;
    text-align: center;
    margin-bottom: 0.2em;
    text-shadow: 0 2px 8px #0002;
  }
  .dashboard-welcome {
    font-size: 1.3rem;
    color: #fffbe6;
    text-align: center;
    margin-bottom: 1.2em;
  }
  .dashboard-section {
    font-size: 1.5rem;
    color: #ffb300;
    font-weight: 700;
    margin-bottom: 2rem;
    text-align: center;
    letter-spacing: 1px;
  }
  .dashboard-cards-row {
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
    justify-content: center;
  }
  .dashboard-card {
    background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
    color: #183642;
    border-radius: 1.5rem;
    box-shadow: 0 4px 24px rgba(25,135,84,0.18);
    padding: 2rem 1.5rem 1.5rem 1.5rem;
    min-width: 320px;
    max-width: 370px;
    flex: 1 1 320px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    margin-bottom: 1.5rem;
    transition: transform 0.15s;
  }
  .dashboard-card:hover {
    transform: translateY(-4px) scale(1.02);
    box-shadow: 0 12px 40px 0 rgba(34,197,94,0.22), 0 2px 8px 0 rgba(0,0,0,0.10);
  }
  .dashboard-card-title {
    font-size: 1.3rem;
    font-weight: bold;
    color: #183642;
    margin-bottom: 0.5em;
  }
  .dashboard-card-desc {
    font-size: 1.1rem;
    color: #183642;
    margin-bottom: 0.7em;
  }
  .dashboard-card-dates {
    font-size: 0.98rem;
    color: #2c5364;
    margin-bottom: 1.2em;
  }
  .dashboard-card-actions {
    display: flex;
    gap: 0.7em;
    margin-top: 1em;
  }
  .dashboard-btn {
    background: #ffe066;
    color: #183642;
    border: none;
    border-radius: 1.2rem;
    font-weight: bold;
    box-shadow: 0 2px 8px rgba(34,197,94,0.10);
    transition: background 0.2s, color 0.2s;
    padding: 0.6em 1em;
    font-size: 1.08rem;
    width: 100%;
    text-align: center;
  }
  .dashboard-btn:hover {
    background: #183642;
    color: #ffe066;
    border: 1.5px solid #ffe066;
  }
  .dashboard-btn-outline {
    background: transparent;
    color: #ffe066;
    border: 1.5px solid #ffe066;
    font-weight: bold;
    border-radius: 1.2rem;
    transition: background 0.2s, color 0.2s;
    padding: 0.6em 1em;
    font-size: 1.08rem;
    width: 100%;
    text-align: center;
  }
  .dashboard-btn-outline:hover {
    background: #ffe066;
    color: #183642;
  }
  .dashboard-empty {
    color: #ffe066;
    background: #22404d;
    border-radius: 1.2rem;
    padding: 1.5em;
    text-align: center;
    font-size: 1.1rem;
    margin-top: 2em;
  }
</style>

<div class="dashboard-bg">
  <div class="dashboard-title">
    <span style="font-size:2.2rem;vertical-align:middle;">&#x1F393;</span> Painel do Aluno
  </div>
  <div class="dashboard-welcome">
    Bem-vindo, <b><%= @aluno.nome %></b>!
    <span style="margin-left:1em; vertical-align:middle; display:inline-block;">
      <%= button_to 'Sair', destroy_aluno_session_path, method: :delete, class: 'dashboard-btn-outline', style: 'padding:0.3em 0.8em; font-size:1em; width:auto; min-width:0;' %>
    </span>
  </div>
  <div class="dashboard-section">Provas Disponíveis</div>
  <div class="dashboard-cards-row">
    <% @provas.each do |prova| %>
      <div class="dashboard-card">
        <div>
          <div class="dashboard-card-title"><%= prova.titulo %></div>
          <div class="dashboard-card-desc"><%= prova.descricao %></div>
          <div class="dashboard-card-dates">Início: <%= prova.data_inicio&.strftime('%d/%m/%Y') %> | Fim: <%= prova.data_fim&.strftime('%d/%m/%Y') %></div>
        </div>
        <div class="dashboard-card-actions">
          <%= button_to 'Iniciar Prova', provalunos_path(prova_id: prova.id), method: :post, class: 'dashboard-btn' %>
          <%= link_to 'Ranking', prova_ranking_path(prova), class: 'dashboard-btn' %>
        </div>
      </div>
    <% end %>
  </div>
  <% if @provas.empty? %>
    <div class="dashboard-empty">Nenhuma prova disponível para seu perfil no momento.</div>
  <% end %>
</div>
