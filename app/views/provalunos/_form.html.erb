<%= form_with(model: provaluno) do |form| %>
  <% if provaluno.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(provaluno.errors.count, "error") %> prohibited this provaluno from being saved:</h2>

      <ul>
        <% provaluno.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :prova_id, style: "display: block" %>
    <%= form.text_field :prova_id %>
  </div>

  <div>
    <%= form.label :aluno_id, style: "display: block" %>
    <%= form.text_field :aluno_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
