<%= form_with(model: serieano) do |form| %>
  <% if serieano.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(serieano.errors.count, "error") %> prohibited this serieano from being saved:</h2>

      <ul>
        <% serieano.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome, style: "display: block" %>
    <%= form.text_field :nome %>
  </div>

  <div>
    <%= form.label :escola_id, style: "display: block" %>
    <%= form.text_field :escola_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
