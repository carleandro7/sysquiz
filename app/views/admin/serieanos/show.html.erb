<div class="container mt-4">
  <h1>Série: <%= @serieano.nome %></h1>
  <p><strong>ID:</strong> <%= @serieano.id %></p>
  <p><strong>Escola:</strong> <%= @serieano.escola&.nome %></p>
  <div class="mt-3">
    <%= link_to 'Editar', edit_admin_serieano_path(@serieano), class: 'btn btn-warning' %>
    <%= link_to 'Voltar', admin_serieanos_path, class: 'btn btn-secondary' %>
  </div>
</div>
