<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Sysquiz" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

  <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css', crossorigin: 'anonymous' %>
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <!-- importmap removed: no longer needed for pure JS timer -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <style>
      body {
        margin: 0;
        font-family: 'Segoe UI', 'Roboto', Arial, sans-serif;
        background: linear-gradient(135deg, #1976d2 0%, #2196f3 100%);
        min-height: 100vh;
        color: #fff;
      }
      header {
        background: rgba(25, 118, 210, 0.95);
        padding: 1.5rem 0;
        text-align: center;
        box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      }
      header h1 {
        margin: 0;
        font-size: 2.5rem;
        letter-spacing: 2px;
        font-weight: 700;
      }
      main {
        max-width: 1200px;
        margin: 2rem auto;
        background: rgba(255,255,255,0.10);
        border-radius: 18px;
        box-shadow: 0 4px 24px rgba(25,118,210,0.12);
        padding: 2rem;
      }
      footer {
        text-align: center;
        padding: 1rem 0;
        color: #bbdefb;
        font-size: 0.95rem;
        margin-top: 2rem;
      }
      /* Example quiz button style */
      .quiz-btn {
        background: #fff;
        color: #1976d2;
        border: none;
        border-radius: 8px;
        padding: 0.75rem 2rem;
        font-size: 1.1rem;
        font-weight: 600;
        cursor: pointer;
        margin: 0.5rem 0;
        transition: background 0.2s, color 0.2s;
      }
      .quiz-btn:hover {
        background: #1976d2;
        color: #fff;
        border: 1px solid #fff;
      }
    </style>
  </head>

  <body>
    <header>
      <div style="margin-top: 1rem;">
        <% if user_signed_in? %>
          <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: 'btn btn-danger btn-sm', data: { turbo_method: :delete, confirm: 'Deseja sair?' } %>
        <% elsif session[:aluno_id] %>
          <%= button_to 'Logout', aluno_logout_path, method: :delete, class: 'btn btn-danger btn-sm', data: { turbo_method: :delete, confirm: 'Deseja sair?' } %>
        <% end %>
      </div>
    </header>
    <main>
      <%= yield %>
    </main>
    <footer>
      &copy; <%= Time.current.year %> Sysquiz. Todos os direitos reservados.
    </footer>
  </body>
</html>
